#include <stdio.h>  

int main (){
// declaração do array(conjuto de dados )num que guarde quatro números inteiros 
    int num [] = {10,25,87,91};
    int pos = 6;
    // imprimir a preimeira posição do array num 
    printf("%d\n",num[pos-5]);

    return 0;
}